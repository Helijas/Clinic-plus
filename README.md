🧪 Clinic Plus — Test Task

Тестовое задание для позиции Web-программист (Frontend + Backend).
Проект выполнен на основе макета Figma и включает верстку, адаптив, работу с PHP/MySQL, AJAX-отправку формы и дополнительные UI-компоненты.

- Главная страница: https://helijas.github.io/Clinic-plus/
- Страница с формой: https://helijas.github.io/Clinic-plus/form.html

🚀 Функционал проекта:

FRONTEND

- Верстка по макету Figma до блока «О клинике».
- Адаптивная сетка (Desktop / Tablet / Mobile).
- Мобильное меню (burger menu).
- Слайдеры Swiper с адаптивными настройками.
- Лайтбокс (просмотр изображений на весь экран).
- Equal-height карточек, responsive изображения.
- Чистый JS (ES Modules, разделение логики).
- Стили разделены на модули (global, header...).

BACKEND

- PHP 8 (OpenServer).
- MySQL для хранения заявок.
- Обработчик формы submit.php:
- валидация имени, почты, телефона;
- проверка повторной заявки за последние 5 минут;
- сохранение в БД;

ООП-архитектура:
- DB.php — класс работы с базой данных;
- FormValidator.php — класс валидации;
- инкапсуляция.

AJAX (Fetch):
Отправка формы без перезагрузки:
- при успехе — скрытие формы и вывод уведомления;
- при ошибке — вывод сообщения над формой.


Структура проекта:

- /clinic-plus
-   index.html
- │  form.html
- │
- ├─ src/
- │   ├─ css/
- │   ├─ js/
- │   ├─ img/
- │   └─ fonts/
- │
- ├─ backend/
- │   DB.php
- │   FormValidator.php
- │   config.php
- │   submit.php
- │
- └─ sql/
-      schema.sql


На сайте реализованы 2 страницы:
- Главная - index.html
- Страница с формой - form.html


Таблица для сохранения заявок:
- CREATE TABLE requests (
-   id INT AUTO_INCREMENT PRIMARY KEY,
-   name VARCHAR(255) NOT NULL,
-   email VARCHAR(255) NOT NULL,
-   phone VARCHAR(50) NOT NULL,
-   created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
- );


🖼️ Lightbox

Функции:
- открытие по клику на превью;
- закрытие:
- по клику вне изображения,
- по кнопке закрытия,
- по кнопке клавиатуры ESC.

Используется нативный JavaScript + CSS-анимации (без библиотек).

🧰 Стек технологий

- HTML5
- CSS3 — модульная структура, адаптивная вёрстка
- JavaScript (ES Modules)
- библиотека Swiper.js
- PHP (ООП)
- MySQL
- Запускался и тестировался локально на OpenServer

📌 Примечания

Проект демонстрирует:
- умение работать с макетом Figma;
- аккуратную структуру кода и файлов;
- реализацию интерактивных элементов (бургер, слайдеры, лайтбокс);
- умение работать с серверной логикой и базой данных;
- AJAX-коммуникацию между фронтендом и PHP-бекендом.
